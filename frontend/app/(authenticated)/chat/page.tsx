/**
 * Chat page — the center panel now shows the intelligence dashboard.
 * PrismChat has moved to the right panel (rendered by RightPanel in AppShell).
 * This page is now just the dashboard route.
 */
export default function ChatPage() {
  return null;
}
