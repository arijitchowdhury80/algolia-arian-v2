# PIP Repo Junk Inventory

**Generated:** 2026-06-28 · ROOT: `/Users/arijitchowdhury/Dropbox/AI-Development/PIP`
**Method:** git status/ls-files for tracked-vs-untracked, `du` for sizes, `grep -rl` for reference checks. Nothing deleted — classification only.

## Headline

- **Tier 1 (safe auto-delete now): ~1.1 GB.** Almost entirely regen-able caches/build output. None of it is tracked in git — all gitignored or untracked.
- **Tier 2 (judgment calls): ~1.1 MB.** Stale superseded docs + duplicated copies + one editor backup. Tracked junk is minimal (CHECKPOINT.md is the only stale *tracked* file).
- **Critical non-junk caught:** `alembic/versions/007_add_company_hierarchy.py` (untracked) is **live app code** — a valid 006→007 migration, NOT junk. `docs/specs/cognitive-stack-architecture.md` is referenced by CLAUDE.md — KEEP.

Confirmed via `git ls-files`: **zero** cache/build artifacts are tracked. The git-tracked tree is clean of junk. All Tier-1 fat is on disk only.

---

## TIER 1 — DEFINITELY JUNK (safe auto-delete)

All gitignored or untracked. Regenerated by the toolchain on next run.

| Path | Size | Rationale |
|---|---|---|
| `frontend/.next/` | **963M** | Next.js build output. Gitignored. Regenerated by `next build`/`dev`. |
| `.mypy_cache/` | **171M** | mypy incremental cache. Gitignored. Regenerated on next `mypy`. |
| `__pycache__/` (×30+ dirs outside `.venv`) | **5.2M** | Python bytecode. Gitignored. Regenerated on import. |
| `.pytest_cache/` | 220K | pytest cache. Gitignored. |
| `.DS_Store` (×25, excl. node_modules/.venv) | 248K | macOS Finder cruft. Gitignored. Present at root + most dirs. |
| `.ruff_cache/` | 124K | ruff cache. Gitignored. |
| `frontend/app/favicon.ico.bak` | 26K | Editor backup of favicon; live `favicon.ico` not present in `app/` (Next 15 may serve it elsewhere) — the `.bak` itself is dead cruft regardless. |
| `frontend/.clerk/.tmp/` | ~0 | Clerk SDK temp dir. Regenerated. |
| `docs/workspace/search-detector-validation/results/_sweep.log` | 52B | Run log from a validation sweep. |
| `.worktrees/` | empty | Empty dir (gitignored worktree mount point). Leave if tooling expects it; harmless either way. |

**Tier-1 reclaimable: ~1.14 GB** (dominated by `.next` 963M + `.mypy_cache` 171M).

> Note: `.venv/` (739M) is the working virtualenv — NOT listed as junk (it's the active env, regen-able but in daily use). Delete only if you intend to `uv sync` fresh.

---

## TIER 2 — LIKELY DEAD (needs a judgment call)

| Path | Tracked? | Size | Evidence it's dead / superseded |
|---|---|---|---|
| `TEMP/` | untracked | 276K | Scratch holding pen. Contains **duplicate copies** of canonical docs: `2026-04-09-prism-v2-implementation-plan.md`, `2026-04-09-prism-unified-architecture-design.md`, `unified-module-architecture.md` — CLAUDE.md points to `docs/plans/` and the vault for these, not TEMP. Also `TEMP/prism-wiki/` (old wiki snapshot, dated 2026-05-08) and `TEMP/audits/` (Nike/OrientalTrading/PetSmart scratch outputs — superseded by VPS audits in `/opt/data/reports` per SESSION.md). **The canonical implementation plan referenced by CLAUDE.md (`docs/plans/2026-04-09-prism-v2-implementation-plan.md`) does NOT exist on disk — only the TEMP copy does.** Before deleting TEMP, either restore that file to `docs/plans/` or fix the CLAUDE.md path, else a referenced doc vanishes. |
| `algolia-search-audit-skill-improvement/` | untracked | 8K | Single file `SESSION.md` from an old one-off skill-improvement session (dir dated Apr 8). Stranded experiment dir at root. No code, no imports. |
| `CHECKPOINT.md` | **TRACKED** | 16K | **Stale tracked file.** "Last updated: 2026-04-02", "Session 11", describes the dead v1 platform ("20 modules built, 1547 tests"). Superseded by `SESSION.md` (current, dated 2026-06-28) per the artifact-roles protocol. Still referenced by `docs/specs/cognitive-stack-architecture.md` and one hermes-prism doc — update those refs if removed. Recommend: archive to vault, then `git rm`. |
| `global-CLAUDE.md` | untracked | 12K | Duplicate of `~/.claude/CLAUDE.md` global constitution (same "Global Operating Constitution" header). **Zero references** anywhere in repo (`grep -rl "global-CLAUDE"` → none). Dead copy of the global config; the real one lives in `~/.claude/`. |
| `AGENTS.md` | untracked | 4K | A `<claude-mem-context>` memory dump dated 2026-05-08 (not an agents-protocol doc). Referenced by `SESSION.md` and one hermes doc, but content is a stale auto-generated memory snapshot, not authored docs. Judgment: stale; verify the two referrers don't depend on its content before removing. |
| `docs/workspace/6-stub-modules/` | untracked | 16K | Single `plan.md`. Stub-module planning workspace. Per CLAUDE.md WORKSPACE rule, completed workspaces should be promoted to vault then deleted. Check if work shipped. |
| `docs/workspace/intel-hiring-scout-phase4/` | untracked | 36K | `_status.md` says **"Status: COMPLETE ✅"**. Shipped workspace — promote reasoning to vault, then delete per WORKSPACE rule. |
| `docs/workspace/phase2-standards-skills/` | untracked | 4K | `_status.md`: "All 4 skills built and live." Done — skills live in `~/.claude/skills/`. Delete-after-promote candidate. |
| `docs/workspace/phase3-thinking-skills/` | untracked | 4K | `_status.md`: "All skills built and live." Done. Same as above. |
| `docs/research/crawl4ai`, `docs/research/n8n` + `docs/plans/2026-05-03-crawl4ai-data-gathering.md` | untracked | (part of 800K+40K) | Crawl4AI/n8n research from an abandoned data-gathering direction (crawl4ai superseded by Scout per memory `project-builtwith-removed-scout-detector` / Scout skill). Likely dead research — confirm crawl4ai isn't the current collector before removing. |

**Tier-2 reclaimable (if all confirmed dead): ~1.1 MB.** Only `CHECKPOINT.md` (16K) is tracked junk.

### Tier-2 cross-cutting risk

`.DS_Store` and `.next`/cache should be **gitignored already** (they are) — but `.DS_Store` keeps regenerating. Consider a `find . -name .DS_Store -delete` housekeeping step. Not a one-time fix.

---

## TIER 3 — KEEP (live)

- **`docs/workspace/hermes-prism-integration/`** (incl. `spike-unify-audit/`) — this session's live work. HARD KEEP.
- **`docs/workspace/ae-journey-research/`** — dated Jun 27, current.
- **`docs/workspace/search-detector-validation/`** (minus the `_sweep.log`) — `_lists.json` + results are the live detector validation set (memory `project-search-detector`).
- **`alembic/versions/007_add_company_hierarchy.py`** — untracked but **live**: valid 006→007 migration in the chain. Application code awaiting commit, not junk.
- **`docs/specs/cognitive-stack-architecture.md`** — untracked but **referenced by CLAUDE.md** (ARCHITECTURE section). KEEP.
- **`docs/superpowers/plans/2026-04-14-company-intel-ui.md`** — untracked; UI plan, judge against current UI direction but not obviously dead.
- **`docs/handoff/2026-06-21-prism-arrie-algolia-handoff.md`** — recent handoff doc.
- `prism_platform/`, `frontend/` (src), `tests/`, `scripts/`, `alembic/`, `pyproject.toml`, `uv.lock`, `docker-compose.yml`, `Makefile`, `CLAUDE.md`, `SESSION.md`, `.env.example` — core project. KEEP.
- `scaffold-project.sh` — tracked; referenced by a hermes-prism design doc as a pattern reference. Low-risk keep (one-time bootstrap script, but harmless and cited).
- `README.md` — empty (0 bytes), tracked. Not junk; should be *filled*, not deleted.

---

## Recommended action order (after review)

1. **Auto-delete Tier 1** (~1.14 GB): `.next`, `.mypy_cache`, `__pycache__`, `.pytest_cache`, `.ruff_cache`, all `.DS_Store`, `favicon.ico.bak`, `_sweep.log`. Zero risk — all regen-able, none tracked.
2. **Before touching TEMP/**: restore `docs/plans/2026-04-09-prism-v2-implementation-plan.md` (or fix the CLAUDE.md pointer) so a referenced doc doesn't break.
3. **`CHECKPOINT.md`**: archive to vault, fix its 2 referrers, then `git rm` — it's the only stale *tracked* file.
4. **Promote-then-delete** the 4 completed workspace dirs (intel-hiring-scout-phase4, phase2/phase3-skills, 6-stub-modules) per the WORKSPACE rule.
5. **Confirm-then-delete** `global-CLAUDE.md` (zero refs), `algolia-search-audit-skill-improvement/`, `AGENTS.md`, crawl4ai/n8n research.
